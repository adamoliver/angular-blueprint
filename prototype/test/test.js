'use strict';

describe('HomeController', function() {

    beforeEach(module('BlueprintApp'));

    var ctrl;
    beforeEach(inject(function($controller) {
        ctrl = $controller('HomeController', {

        });
    }));

    it('should be tested', function(){
        throw "HomeController has no tests!";
    });

});
